// Local dev default. Keep the API URL wired through this file rather than
// hardcoding it elsewhere in the app -- it matters more in a later phase.
window.__ENV__ = {
  API_BASE_URL: "http://localhost:8080",
};
